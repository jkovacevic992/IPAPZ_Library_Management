{% extends 'base.html.twig' %}

{% block body %}
    <div class="row mt-3">
        <div class="col-lg-12">
            <div class="jumbotron">
                <div class="row">
                    <div class="col-lg-3">
                        <p class="text-muted">{{ user.firstName }} {{ user.lastName }}</p>

                    </div>

                </div>
                <p>{{ user.email }}</p>
                <a href="{{ path('employee_change', {'id': user.id}) }}" class="btn btn-primary">Edit</a>
                <a href="{{ path('employee_delete', {'id': user.id}) }}" class="btn btn-danger">Delete Employee</a>


            </div>
        </div>
    </div>


{% endblock %}
