{% extends 'base.html.twig' %}

{% block body %}
    <h3>Customers</h3>
    {% for genre in genres %}
        <div class="row mt-3">
            <div class="col-lg-6">
                <div class="card">
                    <div class="card-body">
                        <h6 class="card-subtitle mt-2 text-muted">{{ genre.name }}</h6>
                        <a href="{{ path('edit_genre', {'id': genre.id}) }}" class="card-link">Edit</a>

                    </div>
                </div>
            </div>
        </div>
    {% endfor %}
{% endblock %}
