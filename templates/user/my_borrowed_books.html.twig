{% extends 'base.html.twig' %}

{% block body %}
    <h3>My Borrowed Books</h3>
    {% for book in books %}
        <div class="row mt-3">
            <div class="col-lg-6">
                <div class="card">
                    <div class="card-body">
        {% for borrowedBook in book.borrowedBooks %}

                            <h6 class="card-subtitle mt-2 text-muted">{{ borrowedBook.book.author }}</h6>
                            <h6 class="card-text">{{ borrowedBook.book.name }}</h6>




        {% endfor %}




                        </h6>
                        {%  if book.paymentMethod != "PayPal" and paymentMethods[0].active %}
                        <h6 class="card-text">Late Fee:
                            {{ lateFee[book.id] }} USD<br>
                        <a href="{{ path('pay',{'id': book.id}) }}" class="btn btn-primary ">Pay</a>
                            {% elseif paymentMethods[0].active == false %}
                                <h6>PayPal payment currently disabled</h6>
                                {% else %}
                            <h6>Late fee paid, you can return your books.</h6>
                       {% endif %}
                    </div>
                </div>
            </div>
        </div>


    {% endfor %}
{% endblock %}
